# 1. Create a tuple with the elements "apple","banana" and "cherry". Print the tuple.
# tpl = ("apple","banana","cherry")
# print(tpl)

# 2. Create a tuple with the elements 1,2,3. Convert the tuple to a list and print the list.
# tpl = (1,2,3)
# lst = list(tpl)
# print(type(lst),lst)

# 3. Create a tuple with the elements "apple","banana" and "cherry". Access the second element of the tuple.
# tpl = ("apple","banana","cherry")
# print(tpl[1])

# 4. Create a tuple with the elements "apple","banana" and "cherry". Print the length of the tuple.
# tpl = ("apple","banana","cherry")
# print(len(tpl))

# 5. Create two tuples, each with three elements. Concatenate the two tuples and print the resulting tuple.
# tpl1 = (1,2,3)
# tpl2 = (4,5,6)
# print(tpl1 + tpl2)

# 6. Create a tuple with the elements 1,2,3,4,5. Slice the tuple to create a new tuple with the elements 2,3,4.
# tpl = (1,2,3,4,5)
# tpl1 = tpl[1:-1]
# print(tpl1)

''' 7. Create a tuple with the elements "apple", "banana", "cherry", "apple".
Find the index of the first occurence of "apple" in the tuple.'''
# tpl = ("apple","banana","cherry","apple")
# print(tpl.index("apple"))

# 8. Create a tuple with the elements "apple", "banana", "cherry".
# Unpack the tuple into three variables called "a", "b", "c". Print the variables.
# tpl = ("apple","banana","cherry")
# a,b,c = tpl[0], tpl[1], tpl[2]
# print(a,b,c)

# 6. Create a tuple with the elements 1,2,3,4. Convert the tuple to a string with the elements separated by commas.
# tpl = (1,2,3,4)
# str = str(tpl)
# print(str)

# 7. Create a tuple with the elements 1,2,3,4. Use the "in" operator to check if the value 3 is in the tuple.
# tpl = (1,2,3,4)
# if(3 in tpl):
#     print("3 is in the tuple")
# else:
#     print("3 is not in the tuple")